An internal note is read for status before it is read for reasoning. The
audience already has the context, so background is dead weight.

Say what changed, whether it is live, and what someone should do differently
now. Keep at most one paragraph of reasoning, and only if the decision would be
argued with otherwise.

Long notes get skimmed to the last line, so put nothing there you do not want
read first.
